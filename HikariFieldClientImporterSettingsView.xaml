<UserControl x:Class="HikariFieldClientImporter.HikariFieldClientImporterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HikariFieldClientImporter"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance local:HikariFieldClientImporterSettingsViewModel}">
    <StackPanel>
        <TextBlock Text="HikariField客户端安装目录："/>
        <TextBox Text="{Binding Settings.ClientInstallPath, Mode=TwoWay}"/>
    </StackPanel>
</UserControl>